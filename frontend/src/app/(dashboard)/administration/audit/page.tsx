import { AuditPage } from "@/features/administration/audit/pages/AuditPage";

export const metadata = { title: "Audit des accès — StockERP" };

export default function Page() {
  return <AuditPage />;
}
